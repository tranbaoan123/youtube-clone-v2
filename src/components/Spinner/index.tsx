const Spinner = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center">
      <div className="w-11 h-11 border-4 border-t-red-500 animate-spin rounded-full"></div>
    </div>
  );
};

export default Spinner;
